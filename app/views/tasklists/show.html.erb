<h1>id: <%= @tasklist.id %>のタスク詳細</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @tasklist.id %></td>
  </tr>
  <tr>
    <th>タイトル</th>
    <td><%= @tasklist.title %></td>
  </tr>
  <tr>
    <th>メッセージ</th>
    <td><%= @tasklist.content %></td>
  </tr>
</table>

<%= link_to'このタスクを編集する',edit_tasklist_path(@tasklist), class: 'btn btn-light'%>
<%= link_to 'このタスクを削除する',@tasklist, method: :delete, data: {confirm: '本当に削除してもよろしいでしょうか？'}, class: 'btn btn-danger'%>
